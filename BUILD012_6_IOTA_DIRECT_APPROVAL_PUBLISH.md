# Build012.6 Iota — Direct Approval Publish

- Removed publication-threshold blocking from the publish endpoint.
- Removed the separate human-override path.
- Clicking Approve is now the final human editorial decision and publishes any complete, validated Writer draft after preflight passes.
- The original Analyze threshold is retained only as metadata for later review.
- GitHub credential normalization from Theta remains included.
